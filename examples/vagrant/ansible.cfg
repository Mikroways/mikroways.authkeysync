[defaults]
# Resuelve el rol mikroways.authkeysync desde el checkout local (este repo está
# en .../mw/mikroways.authkeysync, así que el rol vive en ../../.. ).
# Así la prueba usa el código local, no la versión publicada.
roles_path = ../../..
