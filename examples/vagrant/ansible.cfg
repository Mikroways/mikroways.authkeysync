[defaults]
# The role is resolved through the roles/ symlink (roles/mikroways.authkeysync
# points to the repo root), so the test uses the local code regardless of where
# or under what name the repo is checked out.
roles_path = roles
interpreter_python = /usr/bin/python3
