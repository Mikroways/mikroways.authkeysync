; Inventario de ejemplo. El usuario de conexión es el que nos dieron en cada
; recurso; el rol sincroniza el authorized_keys de ese usuario (sin become).

[bastions]
client-bastion ansible_host=203.0.113.10 ansible_user=mikroways

[resources]
app-01 ansible_host=10.0.0.5 ansible_user=ubuntu
