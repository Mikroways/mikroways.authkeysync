# Activa automáticamente el entorno de uv (ansible, molecule) al entrar al repo.
# Ejecutar una vez: `direnv allow`. Aplica también en subdirectorios (examples/).
[[ -d .venv ]] || uv sync
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
