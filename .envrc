# Automatically activates the uv environment (ansible, molecule) when entering the repo.
# Run once: `direnv allow`. Also applies in subdirectories (examples/).
[[ -d .venv ]] || uv sync
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
