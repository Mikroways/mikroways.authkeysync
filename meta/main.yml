galaxy_info:
  author: Mikroways
  description: Install and configure AuthKeySync to sync a user's authorized_keys from remote sources
  company: Mikroways
  role_name: authkeysync # if absent directory name hosting role is used instead
  namespace: mikroways

  license: GPL-2.0-or-later

  min_ansible_version: "2.19"

  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm

  galaxy_tags:
    - ssh
    - authorizedkeys
    - keys
    - mikroways
    - authkeysync

dependencies: []
